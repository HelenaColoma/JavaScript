function producto(param1, param2){
    return param1*param2;
}

var resultado1=producto(8,9);
var resultado2=producto(54,67);

alert("El valor de la primera multiplicación es: "+resultado1);
alert("El valor de la segunda multiplicación es: "+resultado2);

//si queremos usar el resultado de la función más adelante (en una variable, por ejemplo) le ponemos un return para que el resultado quede grabado.
